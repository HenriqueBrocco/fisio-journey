# Aqui você pluga seu modelo de visão depois (carregamento e inferência)
class VisionService:
    def analyze_frame(self, img_bytes: bytes):
        # TODO: OpenCV/Modelo
        return {"reps": 0, "rom": 0.0}


vision = VisionService()
